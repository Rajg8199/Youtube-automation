{
  "name": "phonewala-gyan",
  "private": true,
  "version": "0.0.0",
  "description": "PhoneWala Gyan — autonomous YouTube content system (monorepo)",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "pnpm -r --if-present run typecheck",
    "test": "pnpm -r --if-present run test",
    "build": "pnpm -r --if-present run build"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
