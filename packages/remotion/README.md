# @pwg/remotion

Programmatic video (Remotion + React). **Empty in Phase 0.** Phase 3 builds 7 named
templates: `spec-card`, `versus-split`, `price-tracker`, `news-banner`, `broll-overlay`,
`chart`, `talking-points`, plus the brand kit (fonts, colors, face-free design system).

ARM note (Oracle Free Tier): verify headless Chrome renders on ARM in Phase 1; document
the x86 fallback if it doesn't. See docs/decisions.md ADR-0005.
